var words = ['sailormoon', 'luna', 'jupitar', "mars", "star"];{

    return word[Math.floor(math.random()*words.length]
}

<input type="test" onkeypress="letterPressed"></input>

function letterPressed(){
    //if letter pressed is correct then put it on the screen and put it in the already used area
    var letter = (" ")
    //if (letter = true);

}
//console.log(words)
//var word = words[Math.floor(Math.random() * words.length)];

//var answerArray = [];
//for (var i = 0; i < word.length; i++) {
//answerArray[i] = "_";
//}



 